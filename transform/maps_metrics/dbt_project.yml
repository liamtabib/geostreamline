name: 'maps_metrics'
version: '1.0.0'
config-version: 2
profile: 'maps_metrics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  maps_metrics:
    +materialized: table